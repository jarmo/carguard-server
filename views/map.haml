:javascript
  $(function() { 
    EncryptedLocations.decryptAll(#{@locations})
      .done(function(locations) {
        new CarGuard(locations).render()
      }).fail(function() {
        alertify.error("Sorry, wrong secret!")
      })
  })

.row
  .col-md-8
    #map
  .col-md-4
    #locations
      %ul
